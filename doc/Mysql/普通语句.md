### 查看数据库列表
```
show databases;
```

### 查看表列表
```
show tables;
```

###  删除表
```
DROP table if EXISTS TEST;
DROP table if EXISTS TEST_USER;
DROP table if EXISTS TEST_ROLE;
```

###  创建表
```
CREATE TABLE TEST_USER (
	id BIGINT (20) auto_increment NOT NULL,
	name VARCHAR (255) NOT NULL,
	password varchar(255) not null,
	realname varchar(255) not null,
	phone varchar(255) not null,
	PRIMARY KEY (id)
) ENGINE = INNODB;

CREATE TABLE TEST_ROLE (
	id BIGINT (20) auto_increment NOT NULL,
	name VARCHAR (255) NOT NULL,
	code varchar(255) not null,
	description varchar(255) not null,
	PRIMARY KEY (id)
) ENGINE = INNODB;
```
```
指定存储引擎时各类数据库可能使用不同key值(TYPE 与 ENGINE)
```

###  新增数据
```
INSERT into TEST_USER(name,password,realname,phone)values('loumt','123456','Mr.Lou','1548461312');
insert into TEST_ROLE(name,code,description)values('管理员','Manager','这是一个管理员角色');
insert into TEST_ROLE(name,code,description)values('待删除用户','Deleter','这是一个管理员角色');
```

### 删除数据
```
delete from TEST_ROLE where name ='待删除用户';
```


### 更新数据
```
update TEST_ROLE set code="CODER",description='1111' where name='管理员';
```

### 查询数据
```
select * from TEST_USER;
1.select * from TEST_USER where name='loumt';
2.select * from TEST_USER where name='LOUMT';
区分大小写:select * from test_user where BINARY name='LOUMT'
```

### 多表查询
```
select u.*,r.* from test_user u,test_role r;
```

### 删除数据
```
delete from user where name='loumt'
```

### 删除表
```
1.DROP TABLE TABLE_NAME;
2.TRUNCATE TABLE_NAME;
```

### 联合查询
```
SELECT
	u.id AS id,
	u.username AS username,
	u.`password` AS `password`,
	u.realname AS realname,
	u.phone AS phone,
	r.role AS role,
	r.description AS description
FROM
	up_role r,
	up_user u,
	up_r_user_role ur
WHERE
	r.id = ur.role_id
AND u.id = ur.user_id
AND u.username = 'loumt';
```